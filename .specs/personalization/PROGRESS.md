# Прогресс: персонализация ассистента

## Done

- Интервью завершено; решения пользователя зафиксированы.
- Созданы feature-спецификация, неочередное задание и журнал решений.
- Выполнена плановая синхронизация правила состава основного контекста со
  спецификацией слоёв памяти.
- Реализация начата и завершена: добавлены модель профиля, API, изоляция
  browser-сеансом, персистентность с обновлением состояния v3→v4, защита
  основного prompt от инъекций и исключение профиля из extractor-контекста.
- Реализованы BFF, типы, клиент и форма управления профилями; покрыты
  автоматическими и browser-проверками built-in и кастомных профилей.

## Next

1. Пользователь проводит итоговый review изменений.
2. Вызывающая сторона принимает решение о commit и доставке изменений.
3. Владелец Human принимает или меняет политику очень длинных предпочтений.

## Blockers

- Блокеров реализации нет.
- **Gap-PER-01, owner: Human:** отсутствует решение о предельном размере
  каждого из трёх свободных текстовых полей и его влиянии на context budget;
  это не блокирует завершённую реализацию MVP.

## Verification

- `gofmt` выполнен.
- `GOCACHE=backend/.gocache go build ./...` — PASS.
- `go test ./internal/memory` — PASS.
- `go test ./internal/httpapi -run TestProfileHandlerCRUDValidationAndIsolation`
  — PASS.
- Полный `GOCACHE=backend/.gocache go test ./...` — PASS по всем пакетам.
- `npm run test` — PASS (42 passed); `npm run lint` — PASS; `npm run typecheck`
  — PASS; `npm run build` — PASS; `npm run test:e2e` — PASS (10 passed).
- Browser-проверки подтверждают встроенные профили, выбор с восстановлением
  после refresh, создание/удаление кастомного профиля и fallback, обязательные
  поля, keyboard-доступность и отсутствие overflow на 390 px и 1440 px.
- Backend-регрессия проверяет точные prompts двух встроенных профилей и
  отсутствие профиля в extractor-контексте.
